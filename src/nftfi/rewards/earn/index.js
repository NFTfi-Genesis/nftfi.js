/**
 * @class
 * Class for working with Earn rewards.
 */
class RewardsEarn {
  allocations;

  constructor(options = {}) {
    this.allocations = options?.allocations;
  }
}

export default RewardsEarn;
